# 📋 Task Board - N-Tier Architecture with Redis

โปรเจกต์ระบบจัดการงาน (Task Board) ที่พัฒนาด้วยสถาปัตยกรรมแบบ N-Tier และทำงานบน Docker Container แบบเต็มรูปแบบ โปรเจกต์นี้ได้รับการอัปเกรดเพื่อรองรับการทำ **Load Balancing** และ **Caching** เพื่อประสิทธิภาพสูงสุด

## 🏗️ Architecture Overview

ระบบประกอบด้วย 4 ส่วนหลัก (Containers):
1. **🌐 Nginx (`taskboard-nginx`):** ทำหน้าที่เป็น Web Server เสิร์ฟหน้าเว็บ Frontend (Light Theme) และเป็น Reverse Proxy / Load Balancer กระจายโหลดไปยัง API
2. **⚙️ Node.js API (`week6-ntier-redis-app`):** Backend REST API (สเกลการทำงานเป็น 3 Instances เพื่อรับโหลดที่มากขึ้น)
3. **⚡ Redis (`taskboard-redis`):** ระบบ In-Memory Cache สำหรับเก็บข้อมูลชั่วคราวและเพิ่มความเร็วในการดึงข้อมูล Tasks
4. **🗄️ PostgreSQL (`taskboard-db`):** ฐานข้อมูลหลัก (Relational Database) สำหรับเก็บข้อมูลแบบถาวร

---

## 🚀 Prerequisites (สิ่งที่ต้องมี)
- [Docker Engine](https://docs.docker.com/engine/install/)
- [Docker Compose](https://docs.docker.com/compose/install/)
- Git & Bash (หรือ WSL สำหรับ Windows)

---

## 🛠️ Installation & Setup (วิธีการติดตั้งและรันโปรเจกต์)

**1. สร้าง SSL Certificate (สำหรับ HTTPS)**
ระบบจำเป็นต้องใช้ SSL Certificate ในการรัน Nginx ให้รันสคริปต์นี้เพื่อสร้าง Certificate จำลอง:
```bash
chmod +x scripts/generate-ssl.sh
./scripts/generate-ssl.sh
```
2. เริ่มต้นระบบด้วย Docker Composeรันคำสั่งด้านล่างเพื่อสร้าง Image และรัน Container ทั้งหมด (โดยกำหนดให้ API ทำงาน 3 ตัว):
```
Bashdocker compose up -d --build --scale app=3
```
3. ตรวจสอบสถานะการทำงานรอประมาณ 10-15 วินาที แล้วเช็คสถานะว่าทุก Container ขึ้น Up (healthy) หรือไม่:
```
Bashdocker compose ps
```
4.เข้าใช้งานแอปพลิเคชันเปิดเว็บเบราว์เซอร์แล้วเข้าไปที่:
```
https://localhost(หมายเหตุ: หากเบราว์เซอร์แจ้งเตือนเรื่องความปลอดภัย ให้กด Advanced > Proceed to localhost)📝 API EndpointsMethodEndpointDescriptionGET/api/healthเช็คสถานะ API, Database และ RedisGET/api/tasksดึงข้อมูลงานทั้งหมด (รองรับ Redis Cache)GET/api/tasks/statsดึงข้อมูลสถิติของงานทั้งหมดPOST/api/tasksสร้างงานใหม่PUT/api/tasks/:idอัปเดตข้อมูล/สถานะ
```
งานDELETE/api/tasks/:idลบงาน🔧 Useful Docker Commands (คำสั่งที่ใช้บ่อย)ดู Logs ของ API ทั้ง 3 ตัว:
```
Bashdocker compose logs -f app
```
ดู Logs ของ Redis
```
Bashdocker compose logs -f redis
```
ปิดระบบทั้งหมด:
```
Bashdocker compose down
```
ลบข้อมูลใน Database (Reset) 
```
Bashdocker compose down -v
```
